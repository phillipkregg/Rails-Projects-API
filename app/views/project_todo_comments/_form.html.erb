<%= form_for([@project, @project_todo, @project_todo_comment]) do |f| %>
  <% if @project_todo_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_todo_comment.errors.count, "error") %> prohibited this project_todo_comment from being saved:</h2>

      <ul>
      <% @project_todo_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="field">
    <%= f.label :project_todo_id %><br>
    <%= f.number_field :project_todo_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
